// Package example contains an executable learning path for flow.
//
// The examples progress from a single typed node through composition, dynamic
// workflows, DAGs, the JSON DSL, data-driven routing, persisted resumption,
// streaming output, conditional graphs, diagrams, and sealed subgraphs. They
// intentionally use only public APIs and are run by "go test", so the
// documentation cannot silently drift away from the library.
//
// This package is instructional. It exports no helpers; application code should
// import flow, flowx, workflow, or an optional workflow subpackage directly.
package example
